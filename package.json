{
  "name": "laserreach-local-agent-mcp",
  "version": "0.8.0",
  "description": "Local MCP, webhook, cron, and self-governed execution helper for Laserreach.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "laserreach-local-agent": "./src/cli.js"
  },
  "files": [
    "src",
    "skills",
    "examples",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "start:mcp": "node src/cli.js mcp",
    "start:serve": "node src/cli.js serve",
    "start:replies": "node src/cli.js replies",
    "capabilities": "node src/cli.js capabilities"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "node-cron": "^4.2.1",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}
